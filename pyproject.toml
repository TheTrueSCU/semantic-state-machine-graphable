[project]
name = "state-machine-graphable"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Richard West", email = "dopplereffect.us@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "graphable>=0.6.1",
    "state-machine",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
state-machine = { git = "https://github.com/TheTrueSCU/state-machine.git" }

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "pytest-gitignore>=1.3",
    "pytest-html-plus>=0.5.2",
    "pytest-isort>=4.0.0",
    "pytest-randomly>=4.0.1",
    "pytest-timeout>=2.4.0",
    "pytest-xdist>=3.8.0",
    "ruff>=0.15.10",
    "ty>=0.0.29",
]
